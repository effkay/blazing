require 'spec_helper'

  describe 'blazing setup' do

    before :each do
      setup_sandbox
    end

    after :each do
      teardown_sandbox
    end

   it 'prepares the repository on the target location'
   it 'configures the repository to allow pushing to the checked out branch'
   it 'runs blazing update for the target'

 end
