require 'spec_helper'

  describe 'blazing update' do

    before :each do
      setup_sandbox
    end

    after :each do
      teardown_sandbox
    end

   it 'generates a post-receive hook based on the current blazing config'
   it 'copies the generated post-receive hook to the target'

 end

