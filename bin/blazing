#!/usr/bin/env ruby -w

require 'optparse'
require 'methadone'
# require 'lib/blazing/config'

include Methadone::Main

main do |command|
  config = Blazing::Config::load(options[:file])
  # Blazing::Runner.exec(command)
end

arg :command
on("-f configuration", "--file configuration", "config file location (defaults to config/blazing.rb)")

go!
